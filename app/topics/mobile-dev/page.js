import { useCallback } from 'react';
import { useRouter } from 'next/navigation';

export default function backEndPage() {
    return (
      <div>
        <h1>Mobile Developer page</h1>
      </div>
    );
  }